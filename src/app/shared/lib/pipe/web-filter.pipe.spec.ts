import { WebFilterPipe } from './web-filter.pipe';

describe('WebFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WebFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
